/**
 * In-app 404 (an expired thread link, a delisted trainer): same branded
 * card as the root boundary, but rendered INSIDE the shell so the
 * header/footer survive the miss.
 */
export { default } from "@/app/not-found";
